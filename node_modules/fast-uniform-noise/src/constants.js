(function() {
"use strict";

const api = {
  SEED: 'seed',
  BUCKETS: 256,
  SAMPLE_SCALE: 1e6,
  SAMPLE_SIZE: 1e8,
  MIN_PRECOMPUTED_OCTAVES: 1,
  MAX_PRECOMPUTED_OCTAVES: 8,
};

module.exports = api;

})();
